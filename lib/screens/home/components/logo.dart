import 'package:flutter/material.dart';

class Longo extends StatelessWidget {
  const Longo({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/banner/Logo.png',width: 350,height: 70,);
  }
}